# 0.2 Hyprland

**TODO**

## 0.1.2 Installing Hyprland

Source: [Install hyprland from scratrch](https://discussion.fedoraproject.org/t/tutorial-fedora-43-install-hyprland-from-scratch/168386)

Needed: Laptop/PC with a minimum of:

- RAM:       4  (GB)

- Storage:  10  (GB)

For further information about the ISO image.

[Everything ISO](https://dl.fedoraproject.org/pub/fedora/linux/development/43/Everything/x86_64/iso)

### Instalation steps

- Step 1: Install base system

- Step 2: Connect to internet (ref - 0.1.0)

- Step 3: Add hyprland copr repo

```
dnf copr enable solopasha/hyprland
```

- Step 4: Download hyprland and ssdm

```
dnf install \
    hyprland \
    sddm \
    tuned \
    tuned-ppd \
    kitty \
    waybar \
    hyprpolkitagent \
    nautilus \
    pavucontrol \
    alsa-sof-firmware \
    alsa-utils \
    blueman \
    NetworkManager-wifi \
    iwl* \
    nm-connection-editor-desktop \
    gvfs \
    gvfs-mtp
```

- Step 5: Enable sddm as the login manager and point systemctl to graphical login.

```
systemctl enable sddm.service
systemctl set-default graphical.target
```

**Important** Reboot after running

- Step 6: Enable hyprpolkit in hyprland.conf to get authentication

Add to waybar and polkit agent to autostart section

```
exec-once = waybar
exec-once = systemctl --user start hyprpolkitagent 
```

#### Optional tools and programs

Azote - Wallpaper setter 

```
dnf install azote swaybg
```

Add it to hyprland conf autostart

```
exec-once = ~/.azotebg-hyprland
```

Disable the default wallpaper in the config by doing 

```
force_default_wallpaper = 1 
disable_hyprland_logo = true 
```

Gnome Software - Software storefront

```
dnf install gnome-software
```

Gimp, Vlc - Image and video support

```
dnf install gimp vlc
```

Firefox, Libreoffice, Thunderbird - Browser, Mail, Office tools

```
dnf install firefox libreoffice thunderbird
```

Flameshot - Screenshot tool

```
sudo dnf install flameshot grim
```

Simplescan - Document scanner support

```
sudo dnf install simple-scan
```

Nwg-Look Gtk tweak to change gtk theme, icons, fonts from a gui

```
sudo dnf install nwg-look
```

